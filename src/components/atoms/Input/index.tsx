import { Input } from "@/material-tailwind/index";
import { InputProps } from "@material-tailwind/react/components/Input/index";

export function InputDefault(props: InputProps) {
  return <Input crossOrigin="" color={props.color} label={props.label} />
}

